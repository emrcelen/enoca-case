package com.enoca.emrecelen.model.enumaration

object Constants {
    enum class Role {
        USER,
        PREMIUM_USER,
        ADMIN
    }

    enum class OrderBy{
        ASC,
        DESC
    }
}